<li>
  <%= line_item.product.name %>
  $<%= line_item.product.price / 100.0 * line_item.quantity %>
  <%= form_with model: line_item, method: :delete do |form| %>
  <%= image_submit_tag "remove-item.svg" %>
  <% end %>
</li>
